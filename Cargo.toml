[package]
name = "RedPack"
version = "8.1.1"
authors = ["jianuo"]
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
num = "0.3.1"


[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"

